import { NavBar } from "../../components/nav";
import { AboutPageContainer } from "./style";

export const AboutPage = () => {
    return (
        <AboutPageContainer>
            <NavBar />
        </AboutPageContainer>
    );
};
